package chess;

public class AlphaBeta {
	
	/*
	 * Determine the max value score for a board configuration.
	 * 
	 * @param board is not null
	 * @param alpha > 0
	 * @param beta > 0
	 * @param 0 < depth < 4
	 * @return  
	 * 
	 */
	private int alphaBetaMax(Board board, float alpha, float beta, int depth) {
		return 0;
	}
	
	/*
	 * Determine the minimum value score for a board configuration.
	 * 
	 * @param board is not null
	 * @param alpha > 0
	 * @param beta > 0
	 * @param 0 < depth < 4
	 * @return  
	 * 
	 */
	private int alphaBetaMin(Board board, float alpha, float beta, int depth) {
		return 0;
	}
	
	/*
	 * Evaluation function
	 */
	private float evaluation() {
		return 0;
	}
	
	public Move chooseMove() {
		return null;
	}
}
